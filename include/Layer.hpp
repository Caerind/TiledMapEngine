#ifndef TME_LAYER_HPP
#define TME_LAYER_HPP

#include <memory>

class Layer
{
    public:
        typedef std::shared_ptr<Layer> Ptr;

        struct Data
        {

        };

        struct Tile
        {

        };

    public:
        Layer();

    private:

};

#endif // TME_LAYER_HPP
